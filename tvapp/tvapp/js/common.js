$(document).ready(function () {

	var $wrapper = $('#wrapper'),
		$header = $('header'),
		hHeader = 0, 
		wWin = $(window).outerWidth(),
		hWin = $(window).outerHeight();

	
});

function l(x){console.log(x);}